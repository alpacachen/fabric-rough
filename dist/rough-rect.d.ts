import { fabric } from "fabric";
import { IRectOptions } from "fabric/fabric-impl";
import { Options } from "roughjs/bin/core";
export declare const RoughRect: {
    new (config: IRectOptions, roughOption?: Options): fabric.Rect;
};
